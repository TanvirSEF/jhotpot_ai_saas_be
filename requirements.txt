# ─────────────────────────────────────────────
# NexusSuite API — Python dependencies
# ─────────────────────────────────────────────

# Core web framework
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4

# Config / environment variables
pydantic-settings>=2.5

# Database (SQLAlchemy 2.0). psycopg2-binary is for PostgreSQL;
# SQLite works out of the box via the stdlib.
SQLAlchemy>=2.0
psycopg2-binary>=2.9

# Security: password hashing + JWT
bcrypt>=4.0
PyJWT>=2.8

# Form data parsing (OAuth2PasswordRequestForm, etc.) + email validation
python-multipart>=0.0.9
email-validator>=2.0
